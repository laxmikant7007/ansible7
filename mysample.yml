---
- hosts: all
  become: yes
  tasks:
          - name: i want install tree
            apt:
                  name: tree
                  state: absent

          - name: i want install wget
            apt:
                  name: wget
                  state: absent
      